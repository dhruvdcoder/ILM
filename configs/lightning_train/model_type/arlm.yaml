# @package _global_

lightning_module:
  _target_: pcdd.diffusion.arlm.ARLMLightningModule

loss:
  _target_: pcdd.diffusion.arlm.ARLMLoss
  loss_on_padding: true

predictor:
  _target_: pcdd.diffusion.arlm.ARLMPredictor
  tokenizer: ${lightning_module:tokenizer}
  noise_schedule: ${lightning_module:noise_schedule}
  max_steps: ??? # depends on the dataset
  max_length: ??? # depends on the dataset
  sampling_method: sample

tags:
  model_type: arlm